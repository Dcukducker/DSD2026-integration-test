# M2 Clinical Web — Integration Test Configuration
# Copy this file to .env and adjust as needed.
#   cp .env.example .env

# V2 Backend API base URL (no trailing slash)
VITE_API_BASE=http://localhost:3000

# Set to "false" to use real V2 backend API instead of mock data
VITE_USE_MOCK=false
